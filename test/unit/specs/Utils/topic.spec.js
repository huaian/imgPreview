import {isCompatibleBrowser, isOuterLink} from '@/Utils/browser' 
import $ from 'jquery' 
import _ from "lodash"
describe('消息机制', () => {
  it('发送消息', () => { // 供异步调用
  })
  it('接受消息', () => { // 供异步调用
  })
})